name: First Workflow
on: [push]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo "This is the first step of the first job."
      - name: Second step
        run: echo "This is the second step of the first job."
      - run: echo "🍏 This job's status is ${{ job.status }}."
